# Dependencies (includes the version-pinned opencode binary — fetched via npm,
# never committed; see README "Prerequisites")
node_modules/

# Scaffolded open-slide workspace — regenerated via `npm run init-slides`.
# Decks the agent writes live here and are treated as user data, not repo source.
slides/

# Local scratch / artifacts
screenshot.png

# Per-user runtime settings (selected model, etc.)
user-settings.json

# Build output (electron-builder)
dist/

# Logs & OS junk
*.log
npm-debug.log*
.DS_Store
Thumbs.db
crop.png
